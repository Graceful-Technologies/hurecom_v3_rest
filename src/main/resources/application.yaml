spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/hurecom_v3
    username: root
    password: root
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: ON
        generate_statistics: true
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  logging:
    level:
      com.gt: DEBUG
      org.hibernate.SQL: DEBUG
      org.hibernate.orm.jdbc.bind: TRACE
  application:
    name: hurecom
  flyway:
    enabled: false
file:
  upload-dir: C:/hurecom-files/uploads
jwt:
  secret: V2hhdEV2ZXJTdXBlclNlY3VyZUtleUdlbmVyYXRlZDEyMzQ1Njc4OTAxMjM0NTY=